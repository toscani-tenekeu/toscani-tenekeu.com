{
  "name": "toscani-tenekeu-dev-resource-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Developer resource hub and publishing platform for toscani-tenekeu.com.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -k -n client,server -c blue,green \"npm run dev --workspace client\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace client",
    "type-check": "npm run type-check --workspace client",
    "lint": "npm run lint:check --workspace client",
    "test": "npm run test:unit --workspace client -- --run && npm test --workspace server",
    "db:init": "npm run db:init --workspace server",
    "start": "npm start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0",
    "npm": ">=10"
  },
  "license": "MIT"
}
